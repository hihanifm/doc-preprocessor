# Optional — copy to .env for local overrides.

# When set, users can POST documents to /support-upload (see UI "Save for developer").
# Relative paths are resolved from the directory containing app.py.
# Leave unset to disable the feature.
# SUPPORT_UPLOAD_DIR=support_uploads
